hdmi_safe=1 # maximum hdmi compatibility and enforce 640x480@60Hz (our playback is max 720p and we want multiple displays so this seems ok)
gpu_mem = 128MB # Not sure whether this really affects anything ...
